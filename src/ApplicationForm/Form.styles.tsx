import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  formHeadline: { color: "#474747" },
  formHeaderDesc: { color: "gray" },
  formHeaderContainer: { marginBottom: theme.spacing(2) },
}));
